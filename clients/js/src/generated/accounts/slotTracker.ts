/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import { Key, KeyArgs, getKeySerializer } from '../types';

export type SlotTracker = Account<SlotTrackerAccountData>;

export type SlotTrackerAccountData = { key: Key; lastSlot: bigint };

export type SlotTrackerAccountDataArgs = { lastSlot: number | bigint };

export function getSlotTrackerAccountDataSerializer(): Serializer<
  SlotTrackerAccountDataArgs,
  SlotTrackerAccountData
> {
  return mapSerializer<SlotTrackerAccountDataArgs, any, SlotTrackerAccountData>(
    struct<SlotTrackerAccountData>(
      [
        ['key', getKeySerializer()],
        ['lastSlot', u64()],
      ],
      { description: 'SlotTrackerAccountData' }
    ),
    (value) => ({ ...value, key: Key.SlotTracker })
  ) as Serializer<SlotTrackerAccountDataArgs, SlotTrackerAccountData>;
}

export function deserializeSlotTracker(rawAccount: RpcAccount): SlotTracker {
  return deserializeAccount(rawAccount, getSlotTrackerAccountDataSerializer());
}

export async function fetchSlotTracker(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<SlotTracker> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'SlotTracker');
  return deserializeSlotTracker(maybeAccount);
}

export async function safeFetchSlotTracker(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<SlotTracker | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeSlotTracker(maybeAccount) : null;
}

export async function fetchAllSlotTracker(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<SlotTracker[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'SlotTracker');
    return deserializeSlotTracker(maybeAccount);
  });
}

export async function safeFetchAllSlotTracker(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<SlotTracker[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) => deserializeSlotTracker(maybeAccount as RpcAccount));
}

export function getSlotTrackerGpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'bglGlyphs',
    'GLYPHQ8TkcUZYrdbMLkfWUzfdKPyCc9JLf987iNY5MAs'
  );
  return gpaBuilder(context, programId)
    .registerFields<{ key: KeyArgs; lastSlot: number | bigint }>({
      key: [0, getKeySerializer()],
      lastSlot: [1, u64()],
    })
    .deserializeUsing<SlotTracker>((account) => deserializeSlotTracker(account))
    .whereField('key', Key.SlotTracker);
}

export function getSlotTrackerSize(): number {
  return 9;
}
