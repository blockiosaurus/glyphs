# RPC endpoint (defaults to devnet)
RPC_URL=https://api.devnet.solana.com

# Path to payer wallet keypair (defaults to ~/.config/solana/id.json)
KEYPAIR_PATH=/path/to/payer-keypair.json

# Optional: Path to vanity keypair for collection address
# Generate with: solana-keygen grind --starts-with GLYPH:1
COLLECTION_KEYPAIR_PATH=/path/to/vanity-keypair.json

# ============================================
# hunt-rare.ts options
# ============================================

# Number of transactions to fire per burst attempt (default: 5)
BURST_COUNT=5

# Delay between each transaction in the burst in ms (default: 200)
# Lower = tighter spread, Higher = wider coverage across slots
BURST_SPREAD_MS=200

# Expected RPC round-trip latency in ms (default: 3000)
# Increase if transactions land after target slot
# Decrease if transactions land before target slot
RPC_LATENCY_MS=3000

# Slot duration in ms (default: 400)
# Mainnet/devnet typically ~400ms, but may vary
SLOT_TIME_MS=400
